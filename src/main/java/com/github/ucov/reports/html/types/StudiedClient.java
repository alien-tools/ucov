package com.github.ucov.reports.html.types;

public record StudiedClient(int studiedClient_id,
                            String studiedClient_type,
                            String studiedClient_name,
                            int studiedClient_nbElement) {
}
